{
    "builders": [
        {
            "type": "docker",
            "image": "alpine",
            "commit": true,
            "run_command": ["-d", "-i", "-t", "{{.Image}}", "/bin/sh"]
        }
  ],

    "provisioners": [
        {
            "type": "shell",
            "scripts": "../../shell/apk/install-ansible/install.sh"
        },
        
        {
            "type": "file",
            "source": "../../ansible",
            "destination": "/tmp"
          },

          {
            "type": "shell",
            "inline": "cd /tmp/ansible && ls -la"
        }
    ],

    "post-processors": [
        [
        {
            "type": "docker-tag",
            "repository": "wolfman.crabdance.com/worker-arm-alpine",
            "tag": "latest"
        }

        
    ]
]



}